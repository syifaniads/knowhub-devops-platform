# KnowHub Backend

Express/TypeScript API with Prisma/PostgreSQL persistence. The active API exposes `/health` and CRUD endpoints for `/posts`.

The historical team repository also contained unused/legacy user-route work. It is not promoted into this canonical active API because the retained frontend authentication was a client-side demo rather than a secure server authentication implementation.
